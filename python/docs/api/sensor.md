# Sensors

Typed detection sensors (motion, object, object assist, face, face embedder, person embedder, segmenter, license-plate, audio, classifier, clip) and smart-home sensors (contact, doorbell, lock, garage, light, switch, ptz, security system, environmental).

::: camera_ui_sdk.sensor
    options:
      members_order: source
      filters:
        - "!^_"
