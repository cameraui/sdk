name: Test

on:
  workflow_call:
    inputs:
      node:
        type: boolean
        default: true
      python:
        type: boolean
        default: true
      go:
        type: boolean
        default: true

permissions:
  contents: read

jobs:
  node:
    if: ${{ inputs.node }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22"

      - name: Install dependencies
        run: |
          npm ci
          npm ci --prefix node

      - name: Lint
        run: npm run lint:node

      - name: Fail if linting produced changes
        run: git diff --exit-code

      - name: Unit tests
        run: npm --prefix node test

  python:
    if: ${{ inputs.python }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: pip install -e "./python[test]" mypy

      - name: Lint (mypy)
        run: cd python && mypy --config-file ./mypy.ini camera_ui_sdk

      - name: Unit tests
        run: cd python && python -m pytest

  go:
    if: ${{ inputs.go }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: "stable"

      - name: Install lint tools (staticcheck + golangci-lint v2)
        run: |
          go install honnef.co/go/tools/cmd/staticcheck@latest
          curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/v2.11.3/install.sh \
            | sh -s -- -b "$(go env GOPATH)/bin" v2.11.3
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: Lint
        run: |
          cd go
          unformatted="$(gofmt -l .)"
          if [ -n "$unformatted" ]; then
            echo "::error::gofmt found unformatted files:"
            echo "$unformatted"
            exit 1
          fi
          go vet ./...
          staticcheck ./...
          golangci-lint run ./...

      - name: Unit tests
        run: cd go && go test ./...
